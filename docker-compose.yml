services:
  redis:
    image: redis:alpine
    container_name: bank_sampah_redis
    ports:
      - "6380:6379"
    volumes:
      - bank_sampah_redis_data:/data
    restart: unless-stopped
    command: redis-server --requirepass @BankSampah1234

volumes:
  bank_sampah_redis_data:
